; Definitions and/or default values for global variables

;A useful constant
pi=acos(-1.)

; Confirmation for set parameters
doask=0
; Do not erase before new plot
noerase=0
; Randomize vector positions for VEL every time
velrandom=0
; Array subtracted from w during animation
wsubtract=0

; File descriptors
logfilename=''
filename=''
physics=''

; Parameters for the functions to be plotted
func=''
plotmode=''
plottitle='default'
autorange='y'
axistype='coord'

; multiplot=0 gives the default number of subplots depending on nfile,nfunc
; multiplot=[3,2,0] defines 3 by 2 subplots filled up in vertical order
; multiplot=[2,4,1] defines 2 by 4 subplots filled up in horizontal order
multiplot=0

; Number of info items on bottom and header lines
headerline=2
bottomline=2

; Parameters for surface and shade_surf
ax=30
az=30

; Parameter for contour and contfill
contourlevel=30

; Parameters for the VEL subroutine
velvector=200
velspeed =5
velpos   =0

; Animation parameters for the movie
firstpict=1
dpict=1
npictmax=151
npict=0

; Slicing parameters
firstslice=1
dslice=1
nslicemax=50
slicedir=0

; Parameters for saving the movie into GIF or PS files
nplotmax=151
savemovie='n'
printmovie='n'

; Transformation parameters for irregular grids
transform=''
dotransform=''
nxreg=[0,0]
nvector=0
vectors=0

; Arrays for cutting out part of the grid
cut=0
grid=0

; added for AMR purposes
noautodomain=0
domainset=0

end
