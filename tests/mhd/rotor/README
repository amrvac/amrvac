MHD rotor test in 2D

GLM is activated, to deactivate, change the definitions.h to:
&patdef('IFNOGLM'  , 1 );

setup with:
$AMRVAC_DIR/setup.pl -d=22 -g=12,12 -p=mhd -u=testmhd -arch=default

run with:
./amrvac -i glmGamma.par &
