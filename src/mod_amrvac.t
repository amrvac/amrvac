!> This module contains modules that users could want to have in their mod_usr.t
!> code, so that they don't have to include them individually.
module mod_amrvac
  use mod_global_parameters
  use mod_usr_methods
  use mod_geometry

  implicit none
  public

contains

end module mod_amrvac
